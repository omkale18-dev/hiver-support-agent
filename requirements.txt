pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Optional -- only needed if you set ANTHROPIC_API_KEY / OPENAI_API_KEY to run
# the real-model path instead of the offline mock (see README "Data & modes").
# anthropic>=0.34
# openai>=1.40
